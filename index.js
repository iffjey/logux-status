var attention = require('./attention')
var confirm = require('./confirm')
var log = require('./log')
var favicon = require('./favicon')

module.exports = {
  attention: attention,
  confirm: confirm,
  log: log,
  favicon: favicon
}
